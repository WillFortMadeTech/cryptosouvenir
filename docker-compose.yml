services:
  web:
    build:
      context: ./app 
      dockerfile: Dockerfile
    volumes:
      - ./app:/app 
      - /app/node_modules
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - WATCHPACK_POLLING=true
